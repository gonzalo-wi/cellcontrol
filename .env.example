# Configuración de Ejemplo para CellControl
# Copia este archivo a .env y ajusta los valores según tu entorno

# Entorno de ejecución
# Valores: development, production, test
APP_ENV=development

# Puerto del servidor HTTP
# Por defecto: 8080
HTTP_PORT=8080

# Cadena de conexión a la base de datos MySQL
# Formato: usuario:contraseña@tcp(host:puerto)/nombre_bd?charset=utf8mb4&parseTime=True&loc=Local
DATABASE_DSN=root:password@tcp(localhost:3306)/cellcontrol?charset=utf8mb4&parseTime=True&loc=Local

# Notas:
# - No subas el archivo .env al repositorio (ya está en .gitignore)
# - Ajusta los valores según tu configuración local
# - Para producción, usa variables de entorno del sistema en lugar de archivo .env

